COMPLETE

MODEL requested=gpt-6-astra observed=null source=null

COVERAGE: All seven contract questions, both required perspectives, the specification, six evidence pages, four underlying research reports, CLAUDE.md and targeted sibling implementation/tests/tooling.

LIMITS: Static verification only; no BLE/network actions, implementation, product tests or other seats' debate reports. Target firmware/transport and compressor diagnosis remain unverified. Additional exclusions: none beyond the detailed limits below.

BODY: **REVISE.** The local-BLE approach fits the goal, but its control and progress guarantees need correction before handoff. This recommendation is advice, not plan-gate approval.

1. **Safety.** Keep service-only, strict confirmation and no button. Current ≥1.0 A is a sampled safeguard, not an atomic running interlock. Require valid fresh data even with the override; restore the complete OEM learning instructions. Remove undocumented cancellation. [wiki/integration-plan.md:149](wiki/integration-plan.md:149); [wiki/device-easystart-flex.md:36](wiki/device-easystart-flex.md:36). D-openai-5/6/9.
2. **Protocol.** Command bytes, offsets and whitelist agree with the APK. The mask can retain SuperLearn and discard unknown flags; reject those service inputs. Printable substring matching is not collision-proof. **Supply-chain-partner:** generic ST UUIDs/model bytes cannot authenticate a peripheral. Bound incoming data, validate compatibility, reject ambiguity, and preserve the outgoing whitelist. [wiki/integration-plan.md:77](wiki/integration-plan.md:77); [wiki/ble-transport.md:20](wiki/ble-transport.md:20). D-openai-1/2/3/4/5.
3. **Connection.** Retain connect-on-demand and 60-second default polling; bound retries, lock whole operations and disconnect on every outcome. Define stale timestamps and the three-failure grace precisely. **Zombie-integrations:** users close the OEM app; if they do not, HA may fail to connect and become unavailable. Disable the HA entry for uninterrupted OEM use; never queue writes for later. [wiki/integration-plan.md:108](wiki/integration-plan.md:108); [wiki/device-easystart-flex.md:22](wiki/device-easystart-flex.md:22). D-openai-4/8.
4. **Entities.** The telemetry/diagnostic set is sufficient. Separate the raw request flag from learning completion, preserve raw learned counts, and map unknown status consistently without inventing a fault. [wiki/integration-plan.md:126](wiki/integration-plan.md:126); [wiki/available-data.md:18](wiki/available-data.md:18). D-openai-7/10.
5. **Scope.** Prefer read-only foundation/probe/telemetry first, then control/progress after evidence gates. One PR needs equivalent explicit phases. Do not add raw writes, OTA, FMask/SCPT controls, automatic relearns/power cycles or electrical/proxy changes. Verify framework-free imports and the pinned HA API independently of sibling stubs. [wiki/integration-plan.md:185](wiki/integration-plan.md:185). D-openai-11.
6. **Goal/gaps.** Keep Default Ramp writes outside v1; expose its existing bit read-only and document OEM-directed diagnosis. Framing, security, rotating identity and missing proxy coverage can block operation. Post-relearn semantics require a separate authorized observation. [wiki/integration-plan.md:203](wiki/integration-plan.md:203); [wiki/device-easystart-flex.md:47](wiki/device-easystart-flex.md:47). D-openai-11.
7. **Contradictions.** Ordinary five-start scope conflicts with preserved SuperLearn; request-flag lifetime is unknown; availability definitions conflict; a read-only probe cannot establish post-write behavior. Command encoding/scaling otherwise agree. [wiki/integration-plan.md:20](wiki/integration-plan.md:20); [wiki/integration-plan.md:211](wiki/integration-plan.md:211).

**Strongest objection:** The design already follows two APK reads and flags uncertainty; capable implementers could resolve routine gaps in one PR. However, the acceptance contract currently permits materially different safety/progress behavior.

**Evidence that would change the position:** Corrected invariants and acceptance cases, target/proxy read-only captures, and separately authorized relearn lifecycle observations.

FOLLOWUPS: Plan owner resolves D-openai-1–11 and reruns the plan gate; obtain the two evidence stages separately. No other followups.

Machine report: [schema report](.pollux/reports/debate-spec-openai.json). Detailed evidence follows.

---

**D-openai-1 — HIGH — [wiki/integration-plan.md:77](wiki/integration-plan.md:77)**

Problem: Verified: (m & 0x1C) | 1 preserves SuperLearn bit 4 and drops unknown bits 5–7. Thus 0x10 becomes 0x11 (ReLearn + SuperLearn), and 0x80 becomes 0x01. This conflicts with the ordinary five-start scope at lines 20–21 and SuperLearn non-goal at line 30. The app's UI reconstruction supports the arithmetic, not the safety of all resulting firmware modes (wiki/ble-protocol.md:86, :87, :121; sources/decompiled/jadx/sources/net/microair/easystart/Relearn.java:340, :379). Inference: an OEM-configured hidden mode or future firmware mask can trigger an unintended learning mode or lose an unknown setting.

Exact fix: Keep the documented OEM arithmetic as a pure helper, but reject the relearn service before writing if the fresh mask has any bit in 0xF0 set. Explain that SuperLearn/unknown upper bits require OEM guidance; allow_running must not bypass this. Preserve bits 2–3, clear bit 1 and set bit 0 only for supported inputs. Add service no-write cases for 0x10, 0x14, 0x1F, 0x20 and 0x80; retain helper arithmetic cases as documentation, not proof those service inputs are supported.

**D-openai-2 — HIGH — [wiki/integration-plan.md:95](wiki/integration-plan.md:95)**

Problem: Verified: run() appends binary notifications without a size ceiling or policy for unsolicited data; parsers specify only lower bounds at lines 73–76. UUIDs are generic ST sample values and the app has no authentication (wiki/device-easystart-flex.md:19; wiki/ble-transport.md:20). A known model check exists only in the relearn flow at line 148. Inference: a misbehaving or spoofed peripheral can flood memory/logs or return plausible-looking garbage. An Enum whitelist constrains outgoing syntax, not incoming bytes or identity.

Exact fix: At the client boundary, accept data only for an active command/current connection; cap total binary payload to 20 bytes for ReadLive and 1100 for ReadEEP, reject rather than truncate overflow, require at least 18/909 before successful parsing, and reject binary payload for SMask. Treat these as conservative v1 compatibility limits, not proven wire lengths. Require the expected GATT service and characteristic properties plus strict ASCII EEPROM model validation; permit v1 control only for exact model 398ULBT after a fresh read. Keep unsupported devices read-only/unsupported. Bound and escape logs. Runtime-check Command and integer arguments, reject extraneous read arguments, and never derive commands from device text. Test oversized, unsolicited, malformed-model and spoof-name/UUID inputs. State explicitly that even matching name/address/model cannot authenticate a determined impersonator; the containment guarantee is bounded resource use and only the three whitelisted commands, with SMask reachable only from an explicit validated service call.

**D-openai-3 — HIGH — [wiki/integration-plan.md:81](wiki/integration-plan.md:81)**

Problem: Verified: a short all-printable binary chunk containing Success satisfies the proposed predicate, contradicting line 84's categorical guarantee. The decompile proves substring matching only, not the 64-byte ceiling or printable-only envelope (wiki/ble-transport.md:70, :88; sources/decompiled/apktool/smali/net/microair/easystart/MainActivityKt$gattCallBack$1.smali:112). Inference: an actual completion with CR/LF/NUL could time out; a printable EEPROM chunk could end a read early after the minimum length; a notification containing both Success and Fail has unspecified precedence.

Exact fix: Describe the predicate as provisional, not collision-proof. Use the read-only probe to capture full completion envelopes and response lengths, then specify a bounded full-match grammar for observed variants, including only observed whitespace/terminators. Reject conflicting success/failure tokens and successful reads with invalid command-specific length/model; never interpret arbitrary printable substrings as a verified control acknowledgment. Test printable binary counterexamples, mixed tokens and actual captured envelopes. If a variant remains indistinguishable from data, leave control unsupported rather than claiming a framing guarantee the protocol cannot supply.

**D-openai-4 — HIGH — [wiki/integration-plan.md:147](wiki/integration-plan.md:147)**

Problem: Verified: 'all in one connection' does not require ownership of that connection across the sequence. The only specified lock is per run() at line 95, while a poll disconnects at line 111. The protocol has no command echo/sequence number (wiki/ble-transport.md:84). The sibling explicitly holds its lock across write and verify (../fogmachine-bt/custom_components/fogmachine_bt/fogmachine/client.py:243). Inference: a poll or concurrent service can interleave between EEPROM snapshot, current guard, write and verification, or disconnect the service; after a timeout, late notifications can be attributed to the next command.

Exact fix: Specify one per-device operation lock covering connect, the entire poll or service transaction, state publication and finally-disconnect. Use an internal already-locked command primitive to avoid reentrant deadlock. Scope callbacks/futures to a connection generation; timeout, cancellation, disconnect or malformed framing must poison that connection, fail the pending request and dispose it before another operation starts. Bound the whole command including write_gatt_char, not only the notification wait. Always clean up even if connect/subscribe fails. Add deterministic poll-versus-service, concurrent-service, cancellation-at-each-await and late-old-connection notification tests.

**D-openai-5 — HIGH — [wiki/integration-plan.md:153](wiki/integration-plan.md:153)**

Problem: Verified: checking bit 0 alone accepts 0x03 when 0x01 was requested, or 0x01 when preserving 0x04 required 0x05. Repeated requests while bit 0 is already set also satisfy this assertion without demonstrating a new relearn; the app suppresses unchanged-mask writes (sources/decompiled/jadx/sources/net/microair/easystart/Relearn.java:395). Firmware consumption timing is unknown (wiki/ble-protocol.md:111). Inference: lost/delayed acknowledgments can leave the write applied despite a service error; blind retries could repeat or restart a real operation.

Exact fix: Require readback startup_mask == requested_mask for a verified saved-setting result; compare the read-only fault_mask and SCPT snapshot too and surface unexpected changes without corrective writes. A fresh pre-write bit 0 already set must return 'already requested; new relearn not established' without another SMask, timestamp reset or new-request event. Record an attempted/unknown outcome when a write may have reached the device but acknowledgment or readback fails; explain it may have applied. Never automatically resend or toggle off/on. Reconcile only with fresh reads, and distinguish saved-setting verification from firmware learning. Add wrong-preserved-bit, conflicting-bit, already-set and lost-ack cases; any immediate self-clear variant requires separately validated semantics.

**D-openai-6 — MEDIUM — [wiki/integration-plan.md:160](wiki/integration-plan.md:160)**

Problem: Verified: cancel_relearn is justified solely by uncertainty about bit 0 self-clearing. The evidence establishes a startup setting, not rollback of erased/partly learned data (wiki/ble-protocol.md:109, :120; docs/claude/research/explore-apk-protocol-openai.md:87). normal_mask also clears Default Ramp while preserving No-Pwr-Up-Delay, so its name does not guarantee the app's '(mask & 7) == 0' Normal Operation (wiki/ble-protocol.md:88). Inference: users may mistake this extra write for cancellation/recovery of a relearn already consumed by firmware.

Exact fix: Remove cancel_relearn from v1 architecture, service schema, coordinator triggers and acceptance tests. Remove the v1 public normal_mask helper if it has no caller. Do not auto-clear bit 0 on progress completion. If later evidence justifies a control, specify and name it 'clear relearn request', define exactly which bits may change, require the same confirmation/validation/verification as relearn, and explicitly exclude profile restoration or undo guarantees.

**D-openai-7 — HIGH — [wiki/integration-plan.md:134](wiki/integration-plan.md:134)**

Problem: Verified: relearn_pending promises to stay on until firmware clears bit 0, but that behavior is explicitly unknown at lines 211–212. Learned Starts is only a raw u8 in the app (wiki/available-data.md:18; docs/claude/research/explore-apk-protocol-anthropic.md:213). The spec persists request metadata at line 154 without defining reset detection, completion, restart recovery or stale-data semantics. EEPROM may remain cached for ten minutes (line 111). Inference: a previous learned_starts=5 could be mistaken for completion of a new request; bit 0 could stay set indefinitely or clear before five starts, and 60-second polling can miss an entire 30-second learning run.

Exact fix: Expose bit 0 as a diagnostic 'relearn request flag' with EEPROM sample timestamp, explicitly separate from progress/completion. Keep learned_starts as the unmodified u8 with live sample timestamp, no 0–5 clamp. Persist a request outcome and baseline; after a verified request report progress unconfirmed until fresh post-request evidence establishes a reset/new learning sequence. Mark completion only under a fixture-backed lifecycle rule; otherwise report observed counts without claiming completion. Survive HA restarts and gaps as unknown, never count polls or elapsed cycles. During a pending observed request refresh EEPROM with each successful poll and add a bounded read-only refresh on rediscovery so short powered windows have a chance to be sampled. Test old 5, counts above 5, sticky/self-clearing bits, failed starts and missed cycles. Document sampling gaps and that five learned starts is distinct from the Flex recommendation of eight cycles before limited power.

**D-openai-8 — MEDIUM — [wiki/integration-plan.md:108](wiki/integration-plan.md:108)**

Problem: Verified: releasing after each poll is appropriate for the single-client OEM app, but lines 112–113 simultaneously say availability equals last-poll success and becomes unavailable only after three failures. No first-failure-without-data, retry budget, service-busy, or stale-age policy is given. Local sibling bleak-retry-connector 4.6.3 defaults to four connect attempts, a 20-second connect timeout and 60-second per-attempt safety timeout (../fogmachine-bt/.venv/lib/python3.13/site-packages/bleak_retry_connector/__init__.py:101, :113, :412, :478); its behavior cannot be equated to a 60-second poll deadline. Device power and phone occupancy can both prevent advertising (wiki/device-easystart-flex.md:22; wiki/ha-proxy-coverage.md:26).

Exact fix: Retain connect-on-demand and fetch a fresh HA BLEDevice for each operation. Specify a 20-second overall connect budget with at most two connector attempts, bounded cleanup, no overlapping polls, and the next poll scheduled after the operation finishes; validate/tune these budgets against proxy traces. Back off failed polls up to 600 seconds, resetting on success or a fresh advertisement. Define availability precisely: unavailable until the first valid sample; retain the last sample with its timestamp for failures 1–2; unavailable at failure 3; reset failure count only on a valid read. Services always demand fresh data and fail promptly if unreachable; never queue a write for a future HVAC call. State that users must close the OEM app for HA, and disable the HA entry for uninterrupted OEM diagnostics. If they do not, one client wins the link; HA reports bounded connection failure with app/power/range guidance, never evicts or assumes the OEM app will adapt.

**D-openai-9 — HIGH — [wiki/integration-plan.md:149](wiki/integration-plan.md:149)**

Problem: Verified: current_a is a sampled u16/10 reading, not an interlock; neither APK nor OEM evidence validates 1.0 A as a running classifier or provides an atomic inhibit-start command (wiki/available-data.md:19; wiki/ble-protocol.md:92). Thus 'never write while the compressor runs unless forced' overstates what a sample can prove. The notification at lines 157–158 omits OEM utility-power preference, consistent cooling/heat-pump mode, early-fault exclusions and the separate eight-cycle limited-power recommendation (docs/claude/research/search-easystart-prior-art.md:85; wiki/device-easystart-flex.md:29). Inference: a start between read and write bypasses the claimed guarantee; the shortened power-cycle directions can encourage repeated unsuccessful starts or premature use of limited power.

Exact fix: Keep service-only, confirm exactly true, no button, and the conservative current >= 1.0 A refusal, but describe it as a fresh sampled-current safeguard with an unavoidable race. Missing/invalid/stale current must abort even with allow_running; that override bypasses only the valid-current threshold and requires explicit documentation of unverified running-write behavior. Do not add a mandatory thermostat-off precondition that removes the device's power. Replace the notification with the sourced OEM sequence: disconnect BLE, power down, use utility/adequate power and one HVAC mode, restore, run the compressor >=30 seconds per successful start, allow normal short-cycle protection/thermostat cycling, exclude early-fault attempts, and complete five learning starts; note at least eight cycles before limited-power operation. Present this procedure in service help before invocation as well as after verified acceptance. Never automate power cycling, defeat delays/protections or imply relearn diagnoses the no-start cause.

**D-openai-10 — MEDIUM — [wiki/integration-plan.md:126](wiki/integration-plan.md:126)**

Problem: Verified: the protocol parser produces UNKNOWN(n) at line 71, but the entity offers one 'unknown' enum option; line 135 classifies every unknown code as an active fault. The evidence only calls codes >9 'Not Defined' (wiki/available-data.md:41). Inference: passing UNKNOWN(10) through violates the declared entity vocabulary, while treating an unrecognized code as a known fault invents semantics.

Exact fix: Map all unrecognized raw codes to the one declared sensor option 'unknown' and retain the numeric status_code as a diagnostic attribute. The problem binary sensor is false only for codes 0/2, true for known fault codes 1/3/4/5/6/7/8/9, and unknown for other codes or missing data. Specify these mappings in entity tests. Keep the remaining telemetry/counter entities and read-only settings diagnostics; expose sample timestamps instead of adding speculative diagnostic controls.

**D-openai-11 — HIGH — [wiki/integration-plan.md:203](wiki/integration-plan.md:203)**

Problem: Verified: §11 is titled 'resolve on first live contact (read-only)', but item 6 asks about behavior after a relearn and item 7 asks for the cause of a physical failure. Two reads cannot establish either. The target was not heard on any proxy (wiki/ha-proxy-coverage.md:17), and no GATT properties/security or completion envelope has been captured. Item 1 can break framing, 2/3 can break transport, 4 can break identity, and 5 can make the entire HA path unreachable. Inference: a single mock-green implementation PR could be presented as a working relearn solution before its transport and lifecycle assumptions are tested.

Exact fix: Split delivery into a read-only foundation PR (bounded protocol/client, probe, HA telemetry, tests and packaging) and a subsequent relearn/progress PR after read-only capture fixtures and a separately authorized lifecycle verification plan. If kept in one PR, make these explicit acceptance phases and keep control disabled until the corresponding evidence gate is satisfied. Move §11.6 to a supervised write/lifecycle gate; label §11.7 a diagnostic hypothesis, not something a probe resolves. For unsupported completion/framing or security requirements, stop control and report unsupported; do not bypass authentication, add update commands, or weaken validation. Require actual proxy reachability before claiming HA usability. Address rotation via explicit user-confirmed reconfiguration retaining entry/entity history, never automatic same-name rebinding. Let the BLE backend negotiate MTU; all commands fit the default ATT payload, so no backend-private MTU workaround is a v1 requirement. Keep Default Ramp control out of v1; document OEM-directed diagnosis and expose its existing mask bit read-only.

**Complete contract coverage and reasoning**

1. SAFETY: service-only, strict confirm=true, fresh-current refusal, no button and post-write OEM notification are proportionate. The 1.0 A guard is heuristic, not an atomic compressor interlock; allow_running must never bypass invalid data. Use complete sourced operating conditions and remove unsupported cancel_relearn. Evidence: wiki/integration-plan.md:143, :149, :160; wiki/device-easystart-flex.md:29, :36; findings D-openai-5, D-openai-6, D-openai-9.

2. PROTOCOL: exact unquoted ASCII commands, little-endian offsets, zero-period handling and the three-command structural whitelist agree with the APK evidence. The OEM mask formula is mathematically correct but can retain SuperLearn/drop unknown flags. Printable <=64-byte substring matching is not collision-proof; bound responses, validate runtime arguments and reset connection state after desynchronization. Evidence: wiki/ble-protocol.md:28, :97; wiki/ble-transport.md:70, :84; findings D-openai-1 through D-openai-5.

3. CONNECTION: connect-on-demand/disconnect is preferable to permanent ownership for this single-client, intermittently powered device; 60 seconds is a reasonable starting read interval, not a latency/coverage guarantee. Account for connector retries, cap operation lifetime, serialize transactions, label stale samples and define the three-failure rule. Prefer backend-negotiated MTU; inspect actual proxy behavior later. Evidence: wiki/integration-plan.md:107; ../fogmachine-bt/custom_components/fogmachine_bt/coordinator.py:106; ../fogmachine-bt/.venv/lib/python3.13/site-packages/bleak_retry_connector/__init__.py:101; findings D-openai-4, D-openai-8, D-openai-11.

4. ENTITIES: current/frequency/peak/delay, raw learned starts, total counters and read-only startup/fault/SCPT/RSSI diagnostics cover the available data. Distinguish observed request flag from completed learning, attach sample timestamps, map unknown states consistently and do not invent an unknown fault. Persist request outcome/baseline without claiming a fresh relearn from an old count of five. Evidence: wiki/available-data.md:14, :45, :55; findings D-openai-7 and D-openai-10.

5. SCOPE: prefer two PRs/acceptance phases: read-only protocol/client/probe/HA telemetry first, then confirmed relearn/progress after evidence gates. One PR is acceptable only with those phases and control gating explicit. Implementers must not add raw command inputs, firmware/OTA/FMask/SCPT writes, Default Ramp/SuperLearn controls, automatic retries/relearn/power cycling, proxy reconfiguration, electrical work or cloud calls. Preserve raw evidence and sibling files. Keep Python 3.13/uv, strict typing/lint, synthetic and trace-backed tests; verify direct protocol import without HA in a fresh process. The sibling uses import-path tricks/stubs and suppressions, so copy structure rather than blindly copying its test setup. Evidence: wiki/integration-plan.md:47, :175, :185; ../fogmachine-bt/pyproject.toml:19, :26; ../fogmachine-bt/tests/conftest.py:30; ../fogmachine-bt/tests/test_protocol.py:13; finding D-openai-11.

6. GOAL/GAPS: do not add a Default Ramp write to v1; it is OEM-directed diagnosis and broadens the control surface. Decode/display the existing bit read-only and document that bad learn data is one Stall cause, not a proven cause of no start attempt. §11.1 framing, .2 properties, .3 bonding/MTU, .4 address rotation and .5 coverage can block operation; hedge with strict compatibility gates, backend MTU, manual reconfiguration and real proxy validation. §11.6 needs a separately authorized write/lifecycle observation; .7 needs diagnosis beyond the two reads. Evidence: wiki/device-easystart-flex.md:47; docs/claude/research/search-easystart-prior-art.md:138; wiki/integration-plan.md:203; finding D-openai-11.

7. CONTRADICTIONS: the strongest conflicts are ordinary five-start scope versus retained SuperLearn, guaranteed binary/completion distinction versus a heuristic, bit-0 pending lifetime versus unknown firmware self-clear, last-poll-success availability versus a three-failure grace, and a read-only probe supposedly resolving post-write behavior. Notification omissions and UNKNOWN(n)/unknown mapping are additional concrete mismatches. Exact command bytes, scaling and the ban on fault clearing agree with evidence. Evidence: wiki/integration-plan.md:20, :77, :81, :112, :134, :157, :203; findings D-openai-1, D-openai-3, D-openai-7 through D-openai-11.

zombie-integrations: the OEM app will not negotiate with HA. The human closes it for HA access; per-operation disconnect leaves opportunities for the app, and disabling the HA entry grants uninterrupted OEM diagnostics. If the app remains open, connection failures/timeouts and stale/unavailable HA data are expected; HA must bound retries, release resources, provide app/power/range guidance and never queue a control request or evict the other client. Evidence: wiki/device-easystart-flex.md:22; wiki/ha-proxy-coverage.md:30; finding D-openai-8.

supply-chain-partner: generic ST UUIDs and untrusted name/model bytes are discovery/compatibility hints, not identity proof. Reject unsupported GATT/model/length/framing, bound buffers/logs, discard unsolicited/old-connection traffic, retain the strict outbound whitelist and let only explicit validated service intent produce SMask. A perfect impersonator can still lie about telemetry/readback; do not claim authentication or safety from the current reading alone. Evidence: wiki/ble-transport.md:20; wiki/device-easystart-flex.md:19; findings D-openai-1 through D-openai-5.

**Evidence classification and limitations**

Verified means checked against the supplied local files or local installed source. Firmware outcomes, races, spoofing consequences and sampling loss described as inference are analytical consequences or possible cases, not observed hardware events. Recommendations are proposed specification changes, not implemented fixes.

- Read-only static debate at microair-bt b895a575bf717dade21feba4a49b70b117fea456; no implementation or product test/lint/typecheck run. Only the two authorized report artifacts were written; no BLE, network, cloud, compressor, secret, commit or push actions.
- All seven contract questions, both named perspectives, all six binding evidence pages plus the spec, CLAUDE.md and all four underlying research reports were covered. Sibling review was targeted to component connection/coordinator/config/entity/diagnostics/init behavior, representative tests, import checks/stubs and pyproject; not an exhaustive sibling code audit.
- OEM/manual statements were evaluated from the supplied local research and wiki, without refetching external sources. Target firmware behavior, actual RF coverage/transport/security and the no-start diagnosis remain unverified; app buffer capacities are conservative limits, not proven exact payload sizes.
- Local connector source is installed version 4.6.3 in the sibling environment, not a guarantee about the eventual HA release. No current HA runtime was exercised; require the spec's pinned HA API check and actual entity/lifecycle tests at implementation.
- No other seats' debate report contents were read. Additional coverage exclusions: none beyond those stated. Host model.observed/source/verified_at and effort.effective/source remain null as supplied.

**Verification record**

Delivery checks passed: supplied validate_report.py exited 0 (Validated 1 report(s)); stdlib artifact checks confirmed 457 rendered-summary words, all seven numbered questions, both required perspectives, eleven unique findings, 81 cited path/line references within bounds, matching contract hash, preserved host-owned skeleton fields and both artifact paths. git diff --exit-code and git diff --cached --exit-code passed; HEAD remained b895a575bf717dade21feba4a49b70b117fea456. No product tests were run.

Exact schema-validation command (cwd `../microair-bt`; file set: `.pollux/reports/debate-spec-openai.json` against the supplied schema):

```sh
PYTHONDONTWRITEBYTECODE=1 uv run --offline --no-project --python ../fogmachine-bt/.venv/bin/python python3 -B /private/var/folders/3j/k2414l990w50wyrnm3lk8fkm0000gn/T/runner-specs-runner_token_c7c6eb6f096d1ce5f47d1c730a667738-4a9q7d2m/a51c2106a1f42627fc01c53d1817af7b-v35/skills/dispatch-contract/validate_report.py .pollux/reports/debate-spec-openai.json
```

Artifact consistency checks ran as an inline stdlib program through `uv run --offline --no-project --python ../fogmachine-bt/.venv/bin/python python3 -B -`, reading only the two reports, the supplied skeleton/contract, and cited source files. One initial shorthand sibling test citation lacked its `../fogmachine-bt/` prefix; it was corrected and all checks passed on rerun.

The optional verification skill wrapper referenced missing version 5.0.7; the installed canonical verification-before-completion skill at a local skills cache path was located and read. This did not require approval or leave work incomplete.
